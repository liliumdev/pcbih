pcbih
=====
A simple PHP web application which crawls the Bosnian PC market for cheap components.

Updating
=====
If the vendor websites update (major HTML changes), the crawler files in /crawl/crawlers should be updated accordingly.